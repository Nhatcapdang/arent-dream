interface Profile {
  label: string;
  image: StaticImageData;
  id: string;
  createdAt: string;
}

interface Diary {
  createdAt: string;
  label: string;
  note: string;
  id: string;
}
